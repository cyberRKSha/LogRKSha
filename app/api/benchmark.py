
from fastapi import APIRouter
import json
import os

router = APIRouter(prefix="/api/benchmark", tags=["benchmark"])

# Placeholder data (embedded to avoid gitignore issues during dev)
# In production, this should read from a JSON file generated by the runner.
DEFAULT_RESULTS = {
    "deeplog": {
        "f1": 0.95,
        "precision": 0.96,
        "recall": 0.95,
        "latency": 2,
        "fpr": 0.04
    },
    "logbert": {
        "f1": 0.96,
        "precision": 0.97,
        "recall": 0.96,
        "latency": 15,
        "fpr": 0.03
    },
    "logad": {
        "f1": 0.98,
        "precision": 0.985,
        "recall": 0.975,
        "latency": 5,
        "fpr": 0.01
    }
}

@router.get("/results")
async def get_benchmark_results():
    """
    Returns the comparison metrics between DeepLog, LogBERT, and LogAD.
    """
    # Try to load real results if they exist
    # Adjust path as needed based on where benchmark_runner saves it
    # For now, return default
    return DEFAULT_RESULTS
